export const TOLERANCES = {
  pipeODInches: 0.1,
  nozzleAngleDeg: 5,
  nozzleWarnDeg: 15,
};

// Fallback circumference when CNC header is missing (corresponds to 20" OD pipe)
export const DEFAULT_CIRCUMFERENCE = 62.83185;
